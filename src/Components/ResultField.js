import React from "react"

export default (p) => {
	return (
		<>
			<div className="result_div" style={{ verticalAlign: "bottom", textAlign: "right", fontWeight: "bold", fontSize: "20px" }}>{p.result}</div>
		</>
	)
}
